{
    "compilerOptions": {
      "baseUrl": ".",
      "paths": {
        "@/*": ["./src/*"]
      }
    },
    "include": ["src/**/*"], // Include all files in the src directory
    "exclude": ["node_modules", "dist"] // Exclude generated files and node_modules
  }